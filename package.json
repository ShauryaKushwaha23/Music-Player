{
  "name": "groovebox-music-player",
  "version": "1.0.0",
  "description": "A vintage hippie aesthetic music player with persistent library, EQ, trimming, and full playback controls.",
  "keywords": ["music-player", "web-app", "vintage", "hippie", "audio"],
  "author": "GrooveBox",
  "license": "MIT",
  "scripts": {
    "start": "npx serve . --port 3000 --no-clipboard",
    "dev": "npx live-server --port=3000 --open=index.html",
    "build": "echo 'Static site — no build required. Run: npm start'",
    "preview": "npx serve . --port 3000"
  },
  "devDependencies": {
    "serve": "^14.2.3",
    "live-server": "^1.2.2"
  }
}
